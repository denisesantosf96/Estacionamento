@model X.PagedList.IPagedList<Estacionamento>
@using X.PagedList.Mvc.Core;
@using X.PagedList.Web.Common;

<table class="table">
        <thead class="thead-dark">
        <tr>
            <th>Id</th>
            <th>Veículo</th>

            <th>Vaga</th>
            <th>Manobrista</th>

            <th>Cliente</th>
            <th>Valor</th>

            <th></th>
        </tr>
        </thead>
        <tbody>
            @for (int i = 0; i < Model.Count; i++)
            {
            <tr>
            <td>@Model[i].Id</td>
            <td>@Model[i].Modelo - @Model[i].Placa</td>
            <td>@Model[i].Localizacao - @Model[i].Status</td>
            <td>@Model[i].NomeManobrista</td>
            <td>@Model[i].Nome</td>
            <td>@Model[i].ValorTotal</td>
            <td><input type="button" value="Editar" class="btn btn-dark" onclick="javascript:window.location=`/Estacionamento/Detalhe/${@Model[i].Id}`">
            <input type="button" value="Excluir" class="btn btn-danger" onclick="Excluir(@Model[i].Id)"></td>
            
            </tr>
            }
        
        </tbody>
    </table>

    @Html.PagedListPager(Model, pagina => Url.Action("Index", new { pagina }))